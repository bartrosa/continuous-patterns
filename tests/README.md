# Tests

Tests mirror **`src/continuous_patterns/`** layout: each top-level experiment package has a matching folder (e.g. **`agate_ch/`** for `continuous_patterns.agate_ch`).

Shared cross-package tests can stay in this directory root when added later.
